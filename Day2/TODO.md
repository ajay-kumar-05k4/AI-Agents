# TODO: Debug voice_assistant.py

- [x] Add error handling for pyttsx3.init() in TTS section
- [x] Fix listen() function to catch exceptions from sr.Microphone() and adjust_for_ambient_noise
- [x] Add error handling in run_chain() for llm.invoke()
- [x] Ensure response is a string before speaking in main loop
- [x] Add print statements for debugging errors
